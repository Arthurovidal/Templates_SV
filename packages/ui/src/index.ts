/**
 * @template/ui — Design System partagé
 *
 * Phase 0: Export vide (structure créée)
 * Phase 1: Tokens, composants de base, layout
 * Phase 2: Sections complètes (Hero, Features, Stats, etc.)
 */

// Tokens
export * from './tokens';

// Hooks
export * from './hooks';

// Les composants seront ajoutés en Phase 1
